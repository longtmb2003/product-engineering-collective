import type { ProcessContent } from "@/content/types";

/*
 * PLACEHOLDER. Not approved copy.
 *
 * These strings were previously hardcoded in app/page.tsx, which violated
 * Architecture Specification §13. They are moved here unchanged. The section
 * itself is unspecified; this file is replaced entirely when it is designed.
 */
export const process: ProcessContent = {
  eyebrow: "Process",
  title: "Coming next",
  intro:
    "How an engagement actually runs, from first conversation through to production.",
  placeholderLabel: "Process",
};
